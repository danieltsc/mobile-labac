export const STORAGE_KEYS = {
  contentVersion: '@bacmate/contentVersion',
  user: '@bacmate/user',
  progress: '@bacmate/progress',
  session: '@bacmate/session'
} as const;

export const CONTENT_VERSION = '1.1.0';
